infer_path = ./data/predict.csv

mode = predict
char_embedding_dim = 100
word_embedding_dim = 300
char_hidden_dim = 50
word_hidden_dim = 200
epoch = 200
learning_rate = 0.015
char_model = rnn
word_model = rnn
batch_size = 300
optimizer = Adam
# default GPU is 0th, 1 means the 2nd GPU
GPU = 1
# NER means name entity recognition, when set to False it means POS
NER = True
model_save_path = ./data/PoSTagger.model
data_save_path = ./data/PoSTagger.data
result_save_path = ./data/prediction.csv
